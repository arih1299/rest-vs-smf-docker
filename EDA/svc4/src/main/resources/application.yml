server:
  port: 8084
  tomcat:
          max-threads: 100

#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: demoEDAsvc4

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true
