server:
  port: 8082
  tomcat:
          max-threads: 100

#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: demoRESTsvc2

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true
