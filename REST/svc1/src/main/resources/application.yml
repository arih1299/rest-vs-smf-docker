server:
  port: 8081
  tomcat:
          max-threads: 100

#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: demoRESTsvc1

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true
